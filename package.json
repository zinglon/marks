{
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/**/*.html src/**/*.css src/**/*.json build/ && web-ext --config=web-ext.config.js build --ignore-files \"**/*.ts\"",
    "dev": "tsc --watch & nodemon -e css,html,json --watch src/ --ignore 'src/*.ts' --exec \"npm run copy\"",
    "copy": "copyfiles -u 1 src/**/*.html src/**/*.css src/**/*.json build/"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.26.0",
    "@typescript-eslint/parser": "^5.26.0",
    "copyfiles": "^2.4.1",
    "eslint": "^8.11.0",
    "eslint-config-standard": "^17.0.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-n": "^15.2.0",
    "eslint-plugin-promise": "^6.0.0",
    "nodemon": "^2.0.16",
    "typescript": "^4.7.2",
    "web-ext": "^6.8.0",
    "web-ext-types": "^3.2.1"
  }
}
